@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@{
    ViewData["Title"] = "Home Page";
}


<body>
    <form method="POST">
        <div id="dynamicInput">
            <label for="fieldName">Template Name: </label>
            <input type="text" name="fieldName">
            <br>

            <select name="element">
                <OPTION value="text">Textbox</OPTION>
                <OPTION value="radio">Radio</OPTION>
                <OPTION value="textarea">Text Area</OPTION>
                <OPTION value="checkbox">Check Box</OPTION>
            </select>

            <input type="button" value="Add an option" onClick="addAllInputs('dynamicInput', document.forms[0].element.value);">
        </div>


        <br><br><br>
        <label for="diagnosis">Diagnosis</label>
        <input type="text" name="patientName"><br><br>

        <input type="button" value="Add another field" onclick="addSec();">

        <br>
    </form>
    <input type="button" value="Save Values"> <input type="button" value="Save Template">
    <br>
    <br>
    <a onclick="this.href='data:text/html;charset=UTF-8,'+encodeURIComponent(document.documentElement.outerHTML)" href="#" download="page.html">Download</a> <br /><br />
    Name:<input type="text" name="Name" value="Custon Name">
    <br>
    <button type="button" onclick="downloadIt()">Download</button>

</body>

<script>
        var counterText = 0;
        var counterRadioButton = 0;
        var counterCheckBox = 0;
        var counterTextArea = 0;
        var counter = 0;

        function addAllInputs(divName, inputType) {

            var newdiv = document.createElement('div');

            switch (inputType) {

                case 'text':

                    newdiv.innerHTML = "Field " + (counterText + 1) + " <input type='text' name='myInputs[]'>" + " <input type='text' name='myInputs[]'><br><br>";
                    //newdiv.innerHTML = "Entry " +(counterText+1)+" <input type='text' name='myInputs[]'><br>";
                    counterText++;

                    break;

                case 'radio':

                    newdiv.innerHTML = "Entry " + (counterRadioButton + 1) + " <input type='radio' name='myRadioButtons[]'>";
                    counterRadioButton++;

                    break;

                case 'checkbox':

                    newdiv.innerHTML = "Entry " + (counterCheckBox + 1) + "<input type='text' name='myInputs[]'>" + " <input type='checkbox' name='myCheckBoxes[]'>";
                    counterCheckBox++;

                    break;

                case 'textarea':

                    newdiv.innerHTML = "Entry " + (counterTextArea + 1) + " <textarea name='myTextAreas[]'>type here...</textarea>";
                    counterTextArea++;

                    break;
            }

            document.getElementById(divName).appendChild(newdiv);
        }
        function addSec() {
            var newdiv = document.createElement('div');

            var div = document.getElementById('dynamicInput').innerHTML;
            newdiv.innerHTML = document.getElementById('dynamicInput').innerHTML;
            document.body.appendChild(newdiv);

        }

</script>